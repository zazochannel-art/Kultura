# Android / Gradle
*.iml
.gradle
/build
/captures
.externalNativeBuild
.cxx
local.properties

# IDE
/.idea/
.DS_Store

# Kotlin / JS
.kotlin/
kotlin-js-store/
web/build/

# Node — the Kultura site ships without a build step; package.json only ever
# exists locally/in CI to pull Playwright for the smoke run. The MONEYFLOW app
# under moneyflow/ is a real Next.js project and does have one.
node_modules/
package.json
package-lock.json
!moneyflow/package.json
!moneyflow/package-lock.json
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Secrets & logs
.env
.env.*
!.env.example
server.log
server_err.log

# Release signing — never commit
keystore.properties
*.jks
*.keystore

# MONEYFLOW build output
moneyflow/.next/
moneyflow/out/
moneyflow/next-env.d.ts
moneyflow/.vercel
