# Android / Gradle
*.iml
.gradle
/build
/captures
.externalNativeBuild
.cxx
local.properties

# IDE
/.idea/
.DS_Store

# Kotlin / JS
.kotlin/
kotlin-js-store/
web/build/

# Node — the app ships without a build step; package.json only ever exists
# locally/in CI to pull Playwright for the smoke run.
node_modules/
package.json
package-lock.json
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Secrets & logs
.env
.env.*
!.env.example
server.log
server_err.log

# Release signing — never commit
keystore.properties
*.jks
*.keystore
